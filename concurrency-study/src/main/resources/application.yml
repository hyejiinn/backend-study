server:
  port: 8080

spring:
  application:
    name: concurrency-study
  datasource:
    url: jdbc:mysql://localhost:3306/my_shop2
    username: root
    password: test1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug
